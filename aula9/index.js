// @ts-nocheck

const nome = 'luiz';
const nome1 = "luis";

let nomeAluno; // undefined = nao aponta pra local nenhum na memória
const sobrenomeAluno = null; // nulo = nao aponta pra local nenhum na memória
const aprovado = false; // boolean - valor lógico
